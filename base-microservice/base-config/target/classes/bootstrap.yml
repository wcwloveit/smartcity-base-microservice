spring:
  application:
    name: config

  cloud:
    config:
      server:
        git:
          uri: git@47.100.31.140:zneo/smartcity.git
          username:
          password:

  rabbitmq:
        #addresses: amqp://localhost:5672
        addresses: ${vcap.services.${PREFIX:}rabbitmq.credentials.uri:amqp://${RABBITMQ_HOST:localhost}:${RABBITMQ_PORT:5672}}
        username: alan
        password: alan
